import React from 'react';
import ReactDOM from 'react-dom/client';

const App: React.FC = () => {
  return <div>File Share Example (Coming Soon)</div>;
};

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);

