# @hard-edging/react

React bindings for Hard-Edging: providers, hooks, and devtools that let you build peer-first UIs without thinking about WebRTC handshakes on every render. All state is designed to flow through the mesh, not through some suspiciously global CDN edge cache.


