# @hard-edging/cli

The `hard-edging` CLI scaffolds and runs Hard-Edging applications. It wires together Vite, the broker, and your mesh so you can focus on writing components instead of reinventing build pipelines that secretly want to be CDNs.


