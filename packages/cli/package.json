{
  "name": "@hard-edging/cli",
  "version": "0.0.1",
  "description": "Hard-Edging CLI for creating and running peer-first apps.",
  "bin": {
    "hard-edging": "dist/index.cjs"
  },
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}


